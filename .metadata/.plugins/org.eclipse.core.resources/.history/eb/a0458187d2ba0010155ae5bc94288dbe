package com.app.dto;

public record Beneficary() {



	    public boolean validateBene(String name) {
	        if (name != null && name.equals("james"))
	            return false;
	        else
	            return true;
	    }

	    public boolean equals(Object o) {
	        // 
	        return true;
	    }
	}

	

